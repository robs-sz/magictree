services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_PASSWORD: shop
    ports:
      - "${WT_PORT_DB:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 2s
      retries: 30

  redis:
    image: redis:8
